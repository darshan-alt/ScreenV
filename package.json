{
  "name": "screenv1-workspace",
  "private": true,
  "workspaces": [
    "web",
    "server",
    "extension"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "dev": "npm run dev:web & npm run dev:server"
  }
}
